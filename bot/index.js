const { GatewayIntents, Client} = require('discord.js')
const {
     Client,
     GatewayIntentBits: {
         Guilds,
         GuildMessages,
         MessageContent
     }
},

client.login(process.env.);
